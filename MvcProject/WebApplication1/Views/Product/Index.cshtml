@model WebApplication1.ViewModels.VmCategoryWiseProduct
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-s pull-s">
        <h4>Category Brand</h4>
        <p>
            <a href="/Product/Create" class="btn btn-sm btn-success">Create New</a>
        </p>
        <table class="table">
            <tr>
                <th>
                    Category
                </th>
                <th>
                    Avaiable Proudct
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model.CategoryList)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CategoryName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Quantity)
                    </td>
                    <td>
                        <a href="/Product/Index?id=@item.CategoryId" class="btn btn-sm btn-primary">Details</a> |
                        <a href="/Product/EditMultiple?id=@item.CategoryId" class="btn btn-sm btn-warning">Edit</a>
                        <a href="/Product/DeleteMultiple?id=@item.CategoryId" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure to delete this category with products?')">Delete</a>
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="col-s7 push-s">
        <h4>Prdoucts of @Model.CategoryName</h4>
        <p>
            <a href="/Product/Create" class="btn btn-sm btn-success">Create New</a>
        </p>
        <table class="table">
            <tr>
                <th>
                    Category
                </th>
                <th>
                    Product
                </th>
                <th>
                    Price
                </th>
                <th>
                    ExpireDate
                </th>
                <th>
                    Quantity
                </th>
                <th>
                    ImagePath
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model.ProductList)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CategoryName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ProductName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Price)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ExpireDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Quantity)
                    </td>
                    <td>
                        <img src="@item.ImagePath" style="height:150px" />
                    </td>
                    <td>
                        <a href="/Product/Edit?id=@item.ProductId" class="btn btn-sm btn-warning">Edit</a> |
                        <a href="/Product/Delete?id=@item.ProductId" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure to delete this product?')">Delete</a>
                    </td>

                </tr>
            }
        </table>
    </div>
</div>
